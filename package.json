{
  "name": "behavioral-insight-journal",
  "version": "1.0.0",
  "description": "A behavioral insights journal that tracks habits and actions, analyzes gaps, and recommends improvements",
  "private": true,
  "workspaces": [
    "packages/types",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=packages/types && npm run build --workspace=frontend && npm run build --workspace=backend",
    "build:types": "npm run build --workspace=packages/types",
    "start": "npm run start --workspace=backend",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "test:backend": "npm run test --workspace=backend",
    "test:frontend": "npm run test --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
